# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environments
.venv/
.env

# Runtime logs from scripts/run_stack.ps1
logs/

# Eval runs: every invocation writes a timestamped pair. Keep the curated ones
# (latest.*, tier-b-initial.*) as committed evidence; ignore the rest.
evals/results/20*.json
evals/results/20*.md

# MLflow — local tracking store and the registered router model artefacts.
# models/mlflow_risk_router/ is generated from the committed pickles by
# `make register`, so it is reproducible rather than source.
mlruns/
mlartifacts/
mlflow.db
models/mlflow_risk_router/

# Claude Code local agent state
.claude/

# OS
.DS_Store
Thumbs.db
